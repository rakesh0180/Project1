import React from "react";
import { Row } from "react-bootstrap";

const OpenPositionContainer = () => {
  return (
    <Row>
      <OpenPositionContainer />
    </Row>
  );
};

export default OpenPositionContainer;
